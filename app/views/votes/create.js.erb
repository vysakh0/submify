<% vid = @votable.class.to_s.downcase+'-'+@votable.id.to_s %>
$(".<%=vid %> > .span8 > .vote").html("<%= escape_javascript render('votes/vote_button', votable: @votable) %>");
$(".<%= vid %>  > .span8 > .votes").html("<%= pluralize( @votable.votes.count, "vote") %>");
$(".<%=vid %> > .vote").html("<%= escape_javascript render('votes/vote_button', votable: @votable) %>");
$(".<%= vid %>  > .votes").html("<%= pluralize( @votable.votes.count, "vote") %>");
