<% vid = @flaggable.class.to_s.downcase+'-'+@flaggable.id.to_s %>
$(".<%= vid %>  > .span8 > .flag").html("<%= escape_javascript render('flags/flag_button', flaggable: @flaggable) %>");
$(".<%= vid %>  > .flag").html("<%= escape_javascript render('flags/flag_button', flaggable: @flaggable) %>");
