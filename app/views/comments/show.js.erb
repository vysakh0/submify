<% if @comments.all.count>0 %>
  $(".comment-<%= @comment.id %> > .span8 > #comment-comments").append('<%= escape_javascript(render(partial: 'comments/comment', collection: @comments)) %>');
<% end -%>
  $(".comment-<%= @comment.id %> > .span8 > .comments-form").append('<%= escape_javascript(render(partial: 'shared/comment_form')) %>');
  $(".comment-<%= @comment.id %>  .show_comment").html('<%= pluralize(@comment.comments.count, "reply") %>');
