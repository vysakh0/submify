<% provide(:title, 'Home') %>

<div class="container-fluid">
  <div class="span8">
    <% if signed_in? %>
      <section>
      <%= render 'shared/link_form' %>
      </section>
        <ol class="links">
          <%= render @feed_items %>
        </ol>
        <%= will_paginate @feed_items %>

    <% else %>
      <% cache "front", expires_in: 10.minutes do %>
      <h4> Front Page </h4>
      <ol class="links">
        <%= render @links if @links %>
      </ol>
    <% end %>
  <% end %>
  </div>  
</div>
