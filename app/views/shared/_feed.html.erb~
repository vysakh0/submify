<% if @feed_items.any? %>
  <ol class="links">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
<%= link_to 'Load More', feed_items_path(page: (params[:page].to_i || 1)+1, per_page: params[:per_page] || 15), class: 'hook', remote: true %>

//  <%= will_paginate @feed_items %>
<% end %>	
