<% if @feed_items.all.count>0 %>
$('ul').append('<%= escape_javascript(render(partial: 'tweets')) %>');
$('a.hook').attr('href', '<%= tweets_path(page: (params[:page].to_i || 1)+1, per_page: params[:per_page] || 15) %>').show();
<% else -%>
$('a.hook').hide();
<% end -%>
