<ul class="breadcrumb">
  <li class="links_user">
  <%= link_to pluralize(@user.links.size, 'link') , user_path(@user) %><span class="divider">/</span> </li> 
  <li class="comments_user">
  <%= link_to pluralize(@user.commented.size, 'comment') , commented_user_path(@user) %> <span class="divider">/</span> </li>
  <li class="followers_user">
  <%= link_to pluralize(@user.followers.size, 'follower') , followers_user_path(@user)%> </li>
</ul>
