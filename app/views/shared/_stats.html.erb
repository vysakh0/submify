<% @user ||= current_user %>

<ul class="breadcrumb">
  <li>
  <%= link_to pluralize(@user.links.count, 'link') , ajax_links_user_path(@user) , class:("active" if @page_tab == "links"), remote: true , method: :post %><span class="divider">/</span> </li> 
  <li>
  <%= link_to pluralize(@user.commented.count, 'comment') , ajax_comments_user_path(@user) ,class:("active" if @page_tab == "comments"), remote: true, method: :post %> <span class="divider">/</span> </li> </li>
  <%= link_to pluralize(@user.followers.count, 'follower') , ajax_followers_user_path(@user) , class:("active" if @page_tab == "followers"), remote: true, method: :post %>
</ul>
