$("#follow_form").html("<%= escape_javascript(render 'users/unfollow', user: @user) %>")

$(".followers_user").html('<%= j link_to pluralize(@user.followers.count, 'follower') , followers_user_path(@user) %>')
