<% provide(:title, @user.user_name) %>
<div class="row-fluid">
  <div class="span8">
    <% tab "followers" %>
    <%= render 'profile_stat_nav' %>
    <ul class="users">
      <%= render @users %>
      </ul>
      <ol class="show-commented"></ol>
      <ol class="links"></ol>
      <%= will_paginate @users %>
  </div>
</div>
