<div class="container-fluid">
<% provide(:title, @user.name) %>
<div class="row-fluid">
   <%= hidden_field_tag 'page_tab',"links" %> 
   <span class="span8"><%= render 'profile_stat_nav' %></span>
  <ol class="links">
    <%= render @link_users %>
    </ol>
    <span class="span8"><%= will_paginate @link_users %></span>
  </div>
</div>
