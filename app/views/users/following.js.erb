<% if @users.all.count>0 %>
  $('.users').append('<%= escape_javascript(render @users) %>');

    $('a.kooh').attr('href', '<%= following_user_path(@user,page: (params[:page].to_i || 1)+1, per_page: params[:per_page] || 15) %>').show();
<% else -%>
  $('a.kooh').hide();
<% end -%>
