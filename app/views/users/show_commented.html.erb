<% provide(:title, @user.user_name) %>
<div class="span8">
  <div class="container-fluid">
    <% tab "comments" %>
    <%= render 'profile_stat_nav' %>
      <ol class="links"></ol>
      <ul class="users"></ul>
      <ol class="show-commented">
        <%= render @comments %>
      </ol>
      <%= will_paginate @comments %>
  </div>
</div>
