<% provide(:title, @user.name) %>
<div class="span8">
  <div class="container">
   <%= hidden_field_tag 'page_tab',"comments" %> 
    <%= render 'profile_stat_nav' %>
      <ol class="show-commented">
        <%= render @comments %>
      </ol>
      <%= will_paginate @comments %>
  </div>
</div>
